dou.training.set.csv.file.path=<should be correct path used on different environments>
dou.regression.model.directory.path=<should be correct path used on different environments>
dou.clustering.model.directory.path=<should be correct path used on different environments>

lyrics.training.set.directory.path=/Users/senthuran/msc/bigdata/assignments/ml-genre/mendeley-music-dataset/split/training-dir
lyrics.model.directory.path=/Users/senthuran/msc/bigdata/assignments/ml-genre/mendeley-music-dataset/split/model

# Selection from 5 possible pipelines: LogisticRegressionPipeline, NaiveBayesBagOfWordsPipeline, NaiveBayesTFIDFPipeline, RandomForestPipeline and FeedForwardNeuralNetworkPipeline
lyrics.pipeline=LogisticRegressionPipeline

mnist.training.set.image.file.path=<should be correct path used on different environments>
mnist.training.set.label.file.path=<should be correct path used on different environments>
mnist.test.set.image.file.path=<should be correct path used on different environments>
mnist.test.set.label.file.path=<should be correct path used on different environments>

mnist.training.set.parquet.file.path=<should be correct path used on different environments>
mnist.test.set.parquet.file.path=<should be correct path used on different environments>
mnist.model.directory.path=<should be correct path used on different environments>
mnist.validation.set.directory.path=<should be correct path used on different environments>